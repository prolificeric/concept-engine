{
  "name": "@creatureco/concept-ml-parser",
  "version": "0.4.0",
  "main": "./dist/index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest src",
    "test:watch": "jest src --watchAll",
    "build": "npm run build:lib && npm run build:types",
    "build:lib": "esbuild src/*.ts --platform=node --sourcemap=external --tree-shaking=true --outdir=dist",
    "build:types": "tsc --emitDeclarationOnly"
  }
}
